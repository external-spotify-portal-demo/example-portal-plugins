import {
  createBackendPlugin,
} from '@backstage/backend-plugin-api';

export const teamInsightsBackendPlugin = createBackendPlugin({
  pluginId: 'team-insights',
  register(_env) {
    // Plugin wiring will be added in Task 2
  },
});
